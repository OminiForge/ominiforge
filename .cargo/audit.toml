# cargo audit 的豁免配置（just audit 跑 cargo audit，读这里；与 deny.toml 的
# cargo-deny ignore 是两个独立机制）。
#
# 零 UI 转向后 gpui 及其依赖闭包已移除，此前的 gpui 传递依赖豁免随之失效。
# 新增豁免的规则见 doc/operation/supply-chain.md。
[advisories]
ignore = []
