<script lang="ts">
	// Placeholder. Monitor dashboard is batch two — blocked on the gateway
	// exposing SessionSummary over REST (phase6-plan.md §5).
</script>

<h1>Monitor</h1>
<p class="muted">Dashboard coming once the gateway exposes monitor endpoints.</p>

<style>
	h1 {
		font-size: 18px;
		font-weight: 600;
		margin-bottom: var(--gap-md);
	}
	.muted {
		color: var(--text-muted);
	}
</style>
